# .env.example
# Biometric Guard configuration
# Set to true to enable relaxed security (higher distance tolerance)
RELAXED_SECURITY=False
# Custom distance threshold (float). Default is 0.68.
BIOMETRIC_THRESHOLD=0.68
